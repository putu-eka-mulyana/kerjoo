import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#33a091',
    marginVertical: 5,
    paddingVertical: 10,
    borderRadius: 8,
    alignItems: 'center',
  },
  label: {
    fontWeight: 'bold',
    fontSize: 14,
    lineHeight: 24,
    color: '#fff',
  },
});
