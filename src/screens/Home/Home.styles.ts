import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    paddingHorizontal: 16,
  },
  label: {
    fontWeight: '900',
    fontSize: 24,
    marginTop: 20,
    marginBottom: 30,
    color: '#33a091',
  },
});
